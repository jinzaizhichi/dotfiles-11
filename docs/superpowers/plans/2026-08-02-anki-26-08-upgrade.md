# Anki 26.08 Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Upgrade the portable Ubuntu bootstrap and this machine from Anki 25.09.4 to 26.08.

**Architecture:** Replace the retired launcher artifact with Anki's official direct Linux archive and installer. Keep Anki user data outside the installation path untouched.

**Tech Stack:** Bash, Anki's official Linux tar.zst package, existing shell bootstrap test.

## Global Constraints

- Preserve `~/.local/share/Anki2`.
- Install Anki 26.08 from the official GitHub release.
- Do not add dependencies or new installer abstractions.

---

### Task 1: Update and verify the bootstrap installer

**Files:**
- Modify: `tests/bootstrap.sh`
- Modify: `scripts/optional/setup-japanese-study.sh`

- [ ] Change the bootstrap fixture to expect `anki-26.08-linux-x86_64.tar.zst` and the `anki-linux` extraction directory.
- [ ] Run `bash tests/bootstrap.sh` and confirm it fails against the 25.09 launcher implementation.
- [ ] Keep the existing version marker and upgrade flow, replacing the launcher artifact names with the 26.08 archive and `anki-linux` extraction directory.
- [ ] Run `bash tests/bootstrap.sh` and `shellcheck scripts/optional/setup-japanese-study.sh`.
- [ ] Download and install the official package, then verify `anki --version` reports `26.08`.
