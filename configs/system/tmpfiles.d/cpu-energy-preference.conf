# Keep factory boost available without holding the CPU in its most aggressive
# performance preference while the laptop is plugged in.
w- /sys/devices/system/cpu/cpufreq/policy*/energy_performance_preference - - - - balance_performance
